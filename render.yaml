services:
  - type: web
    name: ciit-backend
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ADMIN_PIN
        value: "7823"
      - key: ALLOWED_ORIGINS
        value: "https://shravannikalje.github.io"
